import Auth from './auth'
import Env from './env'
import Settings from './settings'
import Teams from './teams'

export default {
  auth:     Auth,
  env:      Env,
  settings: Settings,
  teams:    Teams
}
